# Sample Finder v2 — runtime dependencies (analyse + mix-export, geen Demucs)
#
# Kerndependencies (vereist):
librosa
soundfile
numpy
scipy
mutagen

# Optioneel — betere structuuranalyse (beats/downbeats/segmenten via all-in-one).
# Kan tegenstribbelen op Windows (de `natten`-wheel ontbreekt vaak); gebruik dan
# `--beat-backend librosa`. Installeer apart wanneer gewenst:
#   pip install allin1
#
# Buiten pip:
#   * ffmpeg op PATH: brede decode-dekking (m4a/opus/wma/...) + lossy export
#     (mp3/m4a/aac/opus/ogg/wma). PCM/lossless (wav/flac/aiff) gaat via soundfile.
#   * tkinter zit in de Python-stdlib (dubbelklik / interactieve modus).
#
# NB: Demucs/torch zijn er in v2 bewust UIT. Separatie doe je later, in bulk,
# als losse stap op de geexporteerde clips.
